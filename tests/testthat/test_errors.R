context("Conversion and Error Checking")

# load the libraries
library(mlbench)
library(plyr)
library(ggplot2)
library(BestModel)
library(caret)

test_that("Convert a list of models and plot it", {
  expect_equal(0, 0)
})
